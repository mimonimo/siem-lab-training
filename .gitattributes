# Keep LF line endings so shell scripts run correctly on Linux after checkout
# (Windows checkouts must NOT convert these to CRLF, or bash will break).
*.sh   text eol=lf
*.py   text eol=lf
*.xml  text eol=lf
*.html text eol=lf
*.json text eol=lf
*.md   text eol=lf
